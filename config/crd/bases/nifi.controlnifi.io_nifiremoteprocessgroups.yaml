---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: nifiremoteprocessgroups.nifi.controlnifi.io
spec:
  group: nifi.controlnifi.io
  names:
    kind: NiFiRemoteProcessGroup
    listKind: NiFiRemoteProcessGroupList
    plural: nifiremoteprocessgroups
    singular: nifiremoteprocessgroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.transmissionStatus
      name: Transmission
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              adoptionPolicy:
                properties:
                  mode:
                    default: Never
                    enum:
                    - Never
                    - IfExists
                    - AdoptById
                    - AdoptByName
                    type: string
                  nifiId:
                    type: string
                  requireAnnotation:
                    type: boolean
                  reviewBeforeMutate:
                    type: boolean
                type: object
              clusterRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              comments:
                type: string
              communicationsTimeout:
                default: 30 sec
                description: |-
                  CommunicationsTimeout is a NiFi duration (e.g. "30 sec"); it must match NiFi's default so the
                  operator does not report perpetual drift when unset.
                type: string
              deletionPolicy:
                default: Orphan
                enum:
                - Delete
                - Orphan
                type: string
              driftPolicy:
                properties:
                  ignoreFields:
                    items:
                      type: string
                    type: array
                  mode:
                    default: Warn
                    enum:
                    - Ignore
                    - Warn
                    - Reconcile
                    - Fail
                    type: string
                type: object
              localNetworkInterface:
                type: string
              parentProcessGroupRef:
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  root:
                    type: boolean
                type: object
              position:
                properties:
                  x:
                    format: int64
                    type: integer
                  "y":
                    format: int64
                    type: integer
                type: object
              proxy:
                description: Proxy routes site-to-site traffic through an HTTP proxy
                  (HTTP transport only).
                properties:
                  host:
                    minLength: 1
                    type: string
                  passwordSecretRef:
                    description: |-
                      PasswordSecretRef supplies the proxy password. NiFi masks it on read, so it is never compared
                      for drift once set.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  port:
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  user:
                    type: string
                required:
                - host
                - port
                type: object
              reconciliation:
                properties:
                  paused:
                    type: boolean
                type: object
              targetUris:
                description: |-
                  TargetURIs are the site-to-site endpoints of the remote NiFi. NiFi tries them in order and
                  stores them as a single comma-separated value.
                items:
                  type: string
                minItems: 1
                type: array
              transportProtocol:
                default: RAW
                description: |-
                  TransportProtocol selects the site-to-site transport. RAW uses NiFi's socket protocol; HTTP
                  tunnels site-to-site over the NiFi HTTP(S) port (useful through proxies).
                enum:
                - RAW
                - HTTP
                type: string
              yieldDuration:
                default: 10 sec
                description: |-
                  YieldDuration is a NiFi duration (e.g. "10 sec") the RPG waits before being scheduled again
                  after yielding.
                type: string
            required:
            - clusterRef
            - targetUris
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dependencies:
                properties:
                  ready:
                    type: boolean
                  waitingFor:
                    items:
                      type: string
                    type: array
                type: object
              drift:
                properties:
                  differences:
                    items:
                      type: string
                    type: array
                  lastDetectedTime:
                    format: date-time
                    type: string
                  status:
                    type: string
                type: object
              inputPortCount:
                format: int32
                type: integer
              nifiId:
                type: string
              observedGeneration:
                format: int64
                type: integer
              outputPortCount:
                format: int32
                type: integer
              parentProcessGroupId:
                type: string
              ready:
                type: boolean
              revision:
                properties:
                  clientId:
                    type: string
                  version:
                    format: int64
                    type: integer
                type: object
              sync:
                properties:
                  lastAttemptTime:
                    format: date-time
                    type: string
                  lastError:
                    type: string
                  lastSuccessfulTime:
                    format: date-time
                    type: string
                type: object
              targetSecure:
                description: TargetSecure reports whether the remote NiFi requires
                  a secure site-to-site connection.
                type: boolean
              transmissionStatus:
                description: TransmissionStatus reflects whether the RPG is actively
                  transmitting (Transmitting/Stopped).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
