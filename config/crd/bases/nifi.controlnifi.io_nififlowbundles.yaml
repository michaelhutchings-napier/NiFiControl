---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: nififlowbundles.nifi.controlnifi.io
spec:
  group: nifi.controlnifi.io
  names:
    kind: NiFiFlowBundle
    listKind: NiFiFlowBundleList
    plural: nififlowbundles
    singular: nififlowbundle
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              source:
                properties:
                  git:
                    properties:
                      credentials:
                        properties:
                          caSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          clientCertificateSecretKeyRef:
                            description: |-
                              ClientCertificateSecretKeyRef and ClientKeySecretKeyRef present a PEM client certificate
                              for mutual TLS to an HTTPS NiFi Registry or OCI source. They are additive to token or
                              username/password authentication (the certificate authenticates the connection, the
                              token/credentials authenticate the request). Not used for Git.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          clientKeySecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          insecureSkipVerify:
                            type: boolean
                          oidc:
                            description: |-
                              OIDC obtains a bearer token through the OAuth2 client-credentials grant and uses it to
                              authenticate to a NiFi Registry source. Mutually exclusive with token and
                              username/password; registry sources only.
                            properties:
                              audience:
                                description: |-
                                  Audience requested in the token grant, sent as the `audience` form parameter for
                                  providers that require it (Auth0, for example).
                                type: string
                              clientIDSecretKeyRef:
                                description: ClientIDSecretKeyRef holds the OAuth2
                                  client id.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              clientSecretSecretKeyRef:
                                description: ClientSecretSecretKeyRef holds the OAuth2
                                  client secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              scopes:
                                description: Scopes requested in the token grant.
                                items:
                                  type: string
                                maxItems: 32
                                type: array
                              tokenURL:
                                description: TokenURL is the OAuth2 token endpoint
                                  (must be HTTPS).
                                minLength: 1
                                pattern: ^https://
                                type: string
                            required:
                            - clientIDSecretKeyRef
                            - clientSecretSecretKeyRef
                            - tokenURL
                            type: object
                          passwordSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          sshInsecureIgnoreHostKey:
                            description: |-
                              SSHInsecureIgnoreHostKey disables SSH host-key verification. Development only: it exposes
                              the fetch to man-in-the-middle attacks. Prefer sshKnownHostsSecretKeyRef in production.
                            type: boolean
                          sshKnownHostsSecretKeyRef:
                            description: |-
                              SSHKnownHostsSecretKeyRef provides an OpenSSH known_hosts file used to verify the Git
                              server's host key. Required for SSH Git unless sshInsecureIgnoreHostKey is set.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          sshPrivateKeyPassphraseSecretKeyRef:
                            description: SSHPrivateKeyPassphraseSecretKeyRef decrypts
                              an encrypted SSH private key.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          sshPrivateKeySecretKeyRef:
                            description: |-
                              SSHPrivateKeySecretKeyRef selects SSH authentication for an ssh:// or scp-style Git URL
                              (git@host:org/repo.git). The referenced value is a PEM-encoded SSH private key. SSH auth
                              applies to Git sources only.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          tokenSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          usernameSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                        x-kubernetes-validations:
                        - message: username and password must be configured together
                          rule: has(self.usernameSecretKeyRef) == has(self.passwordSecretKeyRef)
                        - message: token and username/password authentication are
                            mutually exclusive
                          rule: '!(has(self.tokenSecretKeyRef) && (has(self.usernameSecretKeyRef)
                            || has(self.passwordSecretKeyRef)))'
                        - message: clientCertificateSecretKeyRef and clientKeySecretKeyRef
                            must be configured together
                          rule: has(self.clientCertificateSecretKeyRef) == has(self.clientKeySecretKeyRef)
                        - message: oidc is mutually exclusive with token and username/password
                            authentication
                          rule: '!(has(self.oidc) && (has(self.tokenSecretKeyRef)
                            || has(self.usernameSecretKeyRef) || has(self.passwordSecretKeyRef)))'
                        - message: sshKnownHostsSecretKeyRef requires sshPrivateKeySecretKeyRef
                          rule: '!has(self.sshKnownHostsSecretKeyRef) || has(self.sshPrivateKeySecretKeyRef)'
                        - message: sshPrivateKeyPassphraseSecretKeyRef requires sshPrivateKeySecretKeyRef
                          rule: '!has(self.sshPrivateKeyPassphraseSecretKeyRef) ||
                            has(self.sshPrivateKeySecretKeyRef)'
                      path:
                        type: string
                      ref:
                        type: string
                      url:
                        minLength: 1
                        type: string
                    required:
                    - url
                    type: object
                  oci:
                    properties:
                      credentials:
                        properties:
                          caSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          clientCertificateSecretKeyRef:
                            description: |-
                              ClientCertificateSecretKeyRef and ClientKeySecretKeyRef present a PEM client certificate
                              for mutual TLS to an HTTPS NiFi Registry or OCI source. They are additive to token or
                              username/password authentication (the certificate authenticates the connection, the
                              token/credentials authenticate the request). Not used for Git.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          clientKeySecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          insecureSkipVerify:
                            type: boolean
                          oidc:
                            description: |-
                              OIDC obtains a bearer token through the OAuth2 client-credentials grant and uses it to
                              authenticate to a NiFi Registry source. Mutually exclusive with token and
                              username/password; registry sources only.
                            properties:
                              audience:
                                description: |-
                                  Audience requested in the token grant, sent as the `audience` form parameter for
                                  providers that require it (Auth0, for example).
                                type: string
                              clientIDSecretKeyRef:
                                description: ClientIDSecretKeyRef holds the OAuth2
                                  client id.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              clientSecretSecretKeyRef:
                                description: ClientSecretSecretKeyRef holds the OAuth2
                                  client secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              scopes:
                                description: Scopes requested in the token grant.
                                items:
                                  type: string
                                maxItems: 32
                                type: array
                              tokenURL:
                                description: TokenURL is the OAuth2 token endpoint
                                  (must be HTTPS).
                                minLength: 1
                                pattern: ^https://
                                type: string
                            required:
                            - clientIDSecretKeyRef
                            - clientSecretSecretKeyRef
                            - tokenURL
                            type: object
                          passwordSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          sshInsecureIgnoreHostKey:
                            description: |-
                              SSHInsecureIgnoreHostKey disables SSH host-key verification. Development only: it exposes
                              the fetch to man-in-the-middle attacks. Prefer sshKnownHostsSecretKeyRef in production.
                            type: boolean
                          sshKnownHostsSecretKeyRef:
                            description: |-
                              SSHKnownHostsSecretKeyRef provides an OpenSSH known_hosts file used to verify the Git
                              server's host key. Required for SSH Git unless sshInsecureIgnoreHostKey is set.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          sshPrivateKeyPassphraseSecretKeyRef:
                            description: SSHPrivateKeyPassphraseSecretKeyRef decrypts
                              an encrypted SSH private key.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          sshPrivateKeySecretKeyRef:
                            description: |-
                              SSHPrivateKeySecretKeyRef selects SSH authentication for an ssh:// or scp-style Git URL
                              (git@host:org/repo.git). The referenced value is a PEM-encoded SSH private key. SSH auth
                              applies to Git sources only.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          tokenSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          usernameSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                        x-kubernetes-validations:
                        - message: username and password must be configured together
                          rule: has(self.usernameSecretKeyRef) == has(self.passwordSecretKeyRef)
                        - message: token and username/password authentication are
                            mutually exclusive
                          rule: '!(has(self.tokenSecretKeyRef) && (has(self.usernameSecretKeyRef)
                            || has(self.passwordSecretKeyRef)))'
                        - message: clientCertificateSecretKeyRef and clientKeySecretKeyRef
                            must be configured together
                          rule: has(self.clientCertificateSecretKeyRef) == has(self.clientKeySecretKeyRef)
                        - message: oidc is mutually exclusive with token and username/password
                            authentication
                          rule: '!(has(self.oidc) && (has(self.tokenSecretKeyRef)
                            || has(self.usernameSecretKeyRef) || has(self.passwordSecretKeyRef)))'
                        - message: sshKnownHostsSecretKeyRef requires sshPrivateKeySecretKeyRef
                          rule: '!has(self.sshKnownHostsSecretKeyRef) || has(self.sshPrivateKeySecretKeyRef)'
                        - message: sshPrivateKeyPassphraseSecretKeyRef requires sshPrivateKeySecretKeyRef
                          rule: '!has(self.sshPrivateKeyPassphraseSecretKeyRef) ||
                            has(self.sshPrivateKeySecretKeyRef)'
                      digest:
                        type: string
                      image:
                        minLength: 1
                        type: string
                      path:
                        description: Path is the snapshot file in the OCI image filesystem
                          and defaults to flow.json.
                        type: string
                      verify:
                        description: Verify requires the image to carry a valid cosign
                          signature before it is materialized.
                        properties:
                          cosignPublicKeySecretRef:
                            description: |-
                              CosignPublicKeySecretRef references a PEM-encoded public key (ECDSA, Ed25519, or RSA, such as
                              a cosign.pub) that must have signed the artifact.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        required:
                        - cosignPublicKeySecretRef
                        type: object
                    required:
                    - image
                    type: object
                  registry:
                    properties:
                      bucketId:
                        minLength: 1
                        type: string
                      credentials:
                        properties:
                          caSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          clientCertificateSecretKeyRef:
                            description: |-
                              ClientCertificateSecretKeyRef and ClientKeySecretKeyRef present a PEM client certificate
                              for mutual TLS to an HTTPS NiFi Registry or OCI source. They are additive to token or
                              username/password authentication (the certificate authenticates the connection, the
                              token/credentials authenticate the request). Not used for Git.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          clientKeySecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          insecureSkipVerify:
                            type: boolean
                          oidc:
                            description: |-
                              OIDC obtains a bearer token through the OAuth2 client-credentials grant and uses it to
                              authenticate to a NiFi Registry source. Mutually exclusive with token and
                              username/password; registry sources only.
                            properties:
                              audience:
                                description: |-
                                  Audience requested in the token grant, sent as the `audience` form parameter for
                                  providers that require it (Auth0, for example).
                                type: string
                              clientIDSecretKeyRef:
                                description: ClientIDSecretKeyRef holds the OAuth2
                                  client id.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              clientSecretSecretKeyRef:
                                description: ClientSecretSecretKeyRef holds the OAuth2
                                  client secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              scopes:
                                description: Scopes requested in the token grant.
                                items:
                                  type: string
                                maxItems: 32
                                type: array
                              tokenURL:
                                description: TokenURL is the OAuth2 token endpoint
                                  (must be HTTPS).
                                minLength: 1
                                pattern: ^https://
                                type: string
                            required:
                            - clientIDSecretKeyRef
                            - clientSecretSecretKeyRef
                            - tokenURL
                            type: object
                          passwordSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          sshInsecureIgnoreHostKey:
                            description: |-
                              SSHInsecureIgnoreHostKey disables SSH host-key verification. Development only: it exposes
                              the fetch to man-in-the-middle attacks. Prefer sshKnownHostsSecretKeyRef in production.
                            type: boolean
                          sshKnownHostsSecretKeyRef:
                            description: |-
                              SSHKnownHostsSecretKeyRef provides an OpenSSH known_hosts file used to verify the Git
                              server's host key. Required for SSH Git unless sshInsecureIgnoreHostKey is set.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          sshPrivateKeyPassphraseSecretKeyRef:
                            description: SSHPrivateKeyPassphraseSecretKeyRef decrypts
                              an encrypted SSH private key.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          sshPrivateKeySecretKeyRef:
                            description: |-
                              SSHPrivateKeySecretKeyRef selects SSH authentication for an ssh:// or scp-style Git URL
                              (git@host:org/repo.git). The referenced value is a PEM-encoded SSH private key. SSH auth
                              applies to Git sources only.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          tokenSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          usernameSecretKeyRef:
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                        x-kubernetes-validations:
                        - message: username and password must be configured together
                          rule: has(self.usernameSecretKeyRef) == has(self.passwordSecretKeyRef)
                        - message: token and username/password authentication are
                            mutually exclusive
                          rule: '!(has(self.tokenSecretKeyRef) && (has(self.usernameSecretKeyRef)
                            || has(self.passwordSecretKeyRef)))'
                        - message: clientCertificateSecretKeyRef and clientKeySecretKeyRef
                            must be configured together
                          rule: has(self.clientCertificateSecretKeyRef) == has(self.clientKeySecretKeyRef)
                        - message: oidc is mutually exclusive with token and username/password
                            authentication
                          rule: '!(has(self.oidc) && (has(self.tokenSecretKeyRef)
                            || has(self.usernameSecretKeyRef) || has(self.passwordSecretKeyRef)))'
                        - message: sshKnownHostsSecretKeyRef requires sshPrivateKeySecretKeyRef
                          rule: '!has(self.sshKnownHostsSecretKeyRef) || has(self.sshPrivateKeySecretKeyRef)'
                        - message: sshPrivateKeyPassphraseSecretKeyRef requires sshPrivateKeySecretKeyRef
                          rule: '!has(self.sshPrivateKeyPassphraseSecretKeyRef) ||
                            has(self.sshPrivateKeySecretKeyRef)'
                      flowId:
                        minLength: 1
                        type: string
                      registryClientRef:
                        properties:
                          name:
                            minLength: 1
                            type: string
                          namespace:
                            type: string
                        required:
                        - name
                        type: object
                      version:
                        type: string
                    required:
                    - bucketId
                    - flowId
                    - registryClientRef
                    type: object
                  snapshot:
                    description: |-
                      Snapshot contains a NiFi RegisteredFlowSnapshot, such as the JSON
                      returned by downloading a versioned process group.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
                x-kubernetes-validations:
                - message: exactly one flow bundle source must be configured
                  rule: '(has(self.git) ? 1 : 0) + (has(self.oci) ? 1 : 0) + (has(self.registry)
                    ? 1 : 0) + (has(self.snapshot) ? 1 : 0) == 1'
              version:
                type: string
            required:
            - source
            type: object
          status:
            properties:
              artifactDigest:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dependencies:
                properties:
                  ready:
                    type: boolean
                  waitingFor:
                    items:
                      type: string
                    type: array
                type: object
              drift:
                properties:
                  differences:
                    items:
                      type: string
                    type: array
                  lastDetectedTime:
                    format: date-time
                    type: string
                  status:
                    type: string
                type: object
              nifiId:
                type: string
              observedGeneration:
                format: int64
                type: integer
              ready:
                type: boolean
              resolvedRevision:
                type: string
              revision:
                properties:
                  clientId:
                    type: string
                  version:
                    format: int64
                    type: integer
                type: object
              sync:
                properties:
                  lastAttemptTime:
                    format: date-time
                    type: string
                  lastError:
                    type: string
                  lastSuccessfulTime:
                    format: date-time
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
