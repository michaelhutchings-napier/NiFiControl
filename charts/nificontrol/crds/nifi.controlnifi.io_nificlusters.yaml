---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: nificlusters.nifi.controlnifi.io
spec:
  group: nifi.controlnifi.io
  names:
    kind: NiFiCluster
    listKind: NiFiClusterList
    plural: nificlusters
    singular: nificluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              additionalEnv:
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              adoptionPolicy:
                properties:
                  mode:
                    default: Never
                    enum:
                    - Never
                    - IfExists
                    - AdoptById
                    - AdoptByName
                    type: string
                  nifiId:
                    type: string
                  requireAnnotation:
                    type: boolean
                  reviewBeforeMutate:
                    type: boolean
                type: object
              api:
                properties:
                  auth:
                    description: |-
                      NiFiAPIAuthSpec selects exactly one authentication mode for the operator's NiFi
                      REST client. mTLS client-certificate, static bearer token, and username/password
                      JWT exchange are mutually exclusive. Combined behaviour (for example presenting a
                      client certificate for transport while exchanging a token for identity) is not yet
                      proven against live NiFi 2.10 and is therefore rejected.
                    properties:
                      bearerTokenSecretKeyRef:
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      clientCertificate:
                        description: |-
                          NiFiAPIClientCertificate references a Secret containing a PEM client certificate and
                          private key that the operator presents to an external NiFi cluster for mTLS client
                          authentication.
                        properties:
                          certKey:
                            default: tls.crt
                            type: string
                          keyKey:
                            default: tls.key
                            type: string
                          secretName:
                            minLength: 1
                            type: string
                        required:
                        - secretName
                        type: object
                      passwordSecretKeyRef:
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      usernameSecretKeyRef:
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                    x-kubernetes-validations:
                    - message: 'configure exactly one of: clientCertificate, bearer
                        token, or username and password'
                      rule: has(self.bearerTokenSecretKeyRef) || (has(self.usernameSecretKeyRef)
                        && has(self.passwordSecretKeyRef)) || has(self.clientCertificate)
                    - message: bearer token authentication is mutually exclusive with
                        the other modes
                      rule: '!(has(self.bearerTokenSecretKeyRef) && (has(self.usernameSecretKeyRef)
                        || has(self.passwordSecretKeyRef) || has(self.clientCertificate)))'
                    - message: mTLS client certificate authentication is mutually
                        exclusive with username/password
                      rule: '!(has(self.clientCertificate) && (has(self.usernameSecretKeyRef)
                        || has(self.passwordSecretKeyRef)))'
                  timeout:
                    type: string
                  tls:
                    properties:
                      caSecretKeyRef:
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      insecureSkipVerify:
                        type: boolean
                      serverName:
                        type: string
                    type: object
                  uri:
                    minLength: 1
                    type: string
                required:
                - uri
                type: object
              coordination:
                properties:
                  electionMaxWait:
                    default: 2 mins
                    type: string
                  zookeeperConnectString:
                    description: Required when replicas is greater than one.
                    minLength: 1
                    type: string
                  zookeeperRootNode:
                    default: /nifi
                    type: string
                required:
                - zookeeperConnectString
                type: object
              deletionPolicy:
                default: Orphan
                enum:
                - Delete
                - Orphan
                type: string
              driftPolicy:
                properties:
                  ignoreFields:
                    items:
                      type: string
                    type: array
                  mode:
                    default: Warn
                    enum:
                    - Ignore
                    - Warn
                    - Reconcile
                    - Fail
                    type: string
                type: object
              image:
                default: apache/nifi:2.10.0
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: PullPolicy describes a policy for if/when to pull a container
                  image
                enum:
                - Always
                - Never
                - IfNotPresent
                type: string
              internalTLS:
                description: |-
                  NiFiClusterInternalTLSSpec configures operator-managed HTTPS and mutual TLS for an
                  Internal (operator-managed) NiFi cluster. Exactly one certificate provider must be
                  selected: an existing cert-manager issuerRef, an operator-managed self-signed CA
                  chain, or externally supplied PKCS12 Secrets.
                properties:
                  certificate:
                    description: Certificate tunes the operator-generated leaf certificates.
                      Ignored in external mode.
                    properties:
                      additionalServerSANs:
                        description: |-
                          AdditionalServerSANs are extra DNS names added to the server/node certificate, for
                          example an Ingress host. Service and wildcard headless DNS names are always included.
                        items:
                          type: string
                        type: array
                      duration:
                        default: 2160h
                        description: Duration is the validity period of the leaf certificates
                          (Go duration form).
                        type: string
                      nodeCommonName:
                        description: |-
                          NodeCommonName overrides the common name of the shared server/node certificate.
                          The resulting NiFi node identity is "CN=<value>".
                        type: string
                      operatorCommonName:
                        description: |-
                          OperatorCommonName overrides the common name of the operator client certificate.
                          The resulting NiFi initial admin identity is "CN=<value>".
                        type: string
                      renewBefore:
                        default: 360h
                        description: RenewBefore is how long before expiry cert-manager
                          renews the leaf certificates.
                        type: string
                    type: object
                  enabled:
                    default: false
                    description: Enabled turns on HTTPS and mutual TLS for the managed
                      StatefulSet.
                    type: boolean
                  external:
                    description: External consumes PKCS12 keystores and CA material
                      supplied outside the operator.
                    properties:
                      clientSecretName:
                        description: |-
                          ClientSecretName is the Secret holding the operator client keystore.p12, ca.crt, plus
                          PEM tls.crt and tls.key used by the operator's mTLS REST client.
                        minLength: 1
                        type: string
                      initialAdminIdentity:
                        description: InitialAdminIdentity is the exact NiFi identity
                          (client certificate subject DN) granted initial admin.
                        minLength: 1
                        type: string
                      keystorePasswordSecretRef:
                        description: KeystorePasswordSecretRef references the password
                          protecting the supplied PKCS12 stores.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      nodeIdentity:
                        description: NodeIdentity is the exact NiFi identity (server/node
                          certificate subject DN) trusted as a cluster node.
                        minLength: 1
                        type: string
                      serverSecretName:
                        description: ServerSecretName is the Secret holding the server/node
                          keystore.p12, truststore.p12, and ca.crt.
                        minLength: 1
                        type: string
                    required:
                    - clientSecretName
                    - initialAdminIdentity
                    - keystorePasswordSecretRef
                    - nodeIdentity
                    - serverSecretName
                    type: object
                  httpsPort:
                    default: 8443
                    description: HTTPSPort is the secure web port exposed by NiFi
                      and the managed Services.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  issuerRef:
                    description: |-
                      IssuerRef references an existing cert-manager Issuer or ClusterIssuer that signs
                      the server and operator-client certificates directly.
                    properties:
                      group:
                        default: cert-manager.io
                        type: string
                      kind:
                        default: Issuer
                        enum:
                        - Issuer
                        - ClusterIssuer
                        type: string
                      name:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  selfSigned:
                    description: |-
                      SelfSigned requests an operator-managed, namespaced two-stage self-signed CA chain
                      (a self-signed Issuer that signs a CA certificate, backing a CA Issuer that signs
                      the leaf certificates).
                    properties:
                      caCommonName:
                        description: CACommonName overrides the common name of the
                          generated CA certificate.
                        type: string
                      caDuration:
                        default: 8760h
                        description: CADuration is the validity period of the CA certificate
                          (Go duration form).
                        type: string
                    type: object
                type: object
                x-kubernetes-validations:
                - message: when internalTLS is enabled, set exactly one of issuerRef,
                    selfSigned, or external
                  rule: '!self.enabled || [has(self.issuerRef), has(self.selfSigned),
                    has(self.external)].exists_one(x, x)'
              jvm:
                properties:
                  heapInitial:
                    default: 1g
                    type: string
                  heapMax:
                    default: 1g
                    type: string
                type: object
              mode:
                default: Internal
                enum:
                - Internal
                - External
                type: string
              reconciliation:
                properties:
                  paused:
                    type: boolean
                type: object
              replicas:
                default: 1
                format: int32
                minimum: 1
                type: integer
              resources:
                description: ResourceRequirements describes the compute resource requirements.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              service:
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    type: object
                  nodePort:
                    format: int32
                    maximum: 65535
                    minimum: 0
                    type: integer
                  port:
                    default: 8080
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  type:
                    default: ClusterIP
                    description: Service Type string describes ingress methods for
                      a service
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                type: object
              storage:
                properties:
                  accessModes:
                    items:
                      type: string
                    type: array
                  enabled:
                    default: true
                    type: boolean
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 10Gi
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: An empty value explicitly disables dynamic provisioning.
                    type: string
                type: object
            type: object
            x-kubernetes-validations:
            - message: api is required when mode is External
              rule: self.mode != 'External' || has(self.api)
            - message: coordination is required when replicas is greater than one
              rule: self.replicas <= 1 || has(self.coordination)
            - message: internalTLS only applies to operator-managed (Internal) clusters
              rule: '!has(self.internalTLS) || !self.internalTLS.enabled || self.mode
                != ''External'''
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dependencies:
                properties:
                  ready:
                    type: boolean
                  waitingFor:
                    items:
                      type: string
                    type: array
                type: object
              drift:
                properties:
                  differences:
                    items:
                      type: string
                    type: array
                  lastDetectedTime:
                    format: date-time
                    type: string
                  status:
                    type: string
                type: object
              endpoint:
                type: string
              nifiId:
                type: string
              observedGeneration:
                format: int64
                type: integer
              ready:
                type: boolean
              revision:
                properties:
                  clientId:
                    type: string
                  version:
                    format: int64
                    type: integer
                type: object
              rootProcessGroupId:
                type: string
              sync:
                properties:
                  lastAttemptTime:
                    format: date-time
                    type: string
                  lastError:
                    type: string
                  lastSuccessfulTime:
                    format: date-time
                    type: string
                type: object
              tls:
                description: NiFiClusterTLSStatus reports the resolved internal TLS
                  materials.
                properties:
                  clientSecretName:
                    description: ClientSecretName is the Secret the operator loads
                      for its mTLS REST client.
                    type: string
                  initialAdminIdentity:
                    description: InitialAdminIdentity is the NiFi identity granted
                      initial admin (operator client DN).
                    type: string
                  issuerKind:
                    description: IssuerKind is Issuer or ClusterIssuer.
                    type: string
                  issuerName:
                    description: IssuerName is the cert-manager issuer that signs
                      the leaf certificates.
                    type: string
                  mode:
                    description: 'Mode is the resolved provider: CertManagerIssuer,
                      SelfSigned, or External.'
                    type: string
                  nodeIdentity:
                    description: NodeIdentity is the NiFi identity trusted as a cluster
                      node (server certificate DN).
                    type: string
                  ready:
                    description: Ready is true once keystore.p12, truststore.p12,
                      and CA material are available.
                    type: boolean
                  serverSecretName:
                    description: ServerSecretName is the Secret consumed by the NiFi
                      StatefulSet.
                    type: string
                type: object
              workload:
                properties:
                  headlessServiceName:
                    type: string
                  readyReplicas:
                    format: int32
                    type: integer
                  replicas:
                    format: int32
                    type: integer
                  serviceName:
                    type: string
                  statefulSetName:
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
