---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: nififlowdeployments.nifi.controlnifi.io
spec:
  group: nifi.controlnifi.io
  names:
    kind: NiFiFlowDeployment
    listKind: NiFiFlowDeploymentList
    plural: nififlowdeployments
    singular: nififlowdeployment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              adoptionPolicy:
                properties:
                  mode:
                    default: Never
                    enum:
                    - Never
                    - IfExists
                    - AdoptById
                    - AdoptByName
                    type: string
                  nifiId:
                    type: string
                  requireAnnotation:
                    type: boolean
                  reviewBeforeMutate:
                    type: boolean
                type: object
              clusterRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              deletionPolicy:
                default: Orphan
                enum:
                - Delete
                - Orphan
                type: string
              driftPolicy:
                properties:
                  ignoreFields:
                    items:
                      type: string
                    type: array
                  mode:
                    default: Warn
                    enum:
                    - Ignore
                    - Warn
                    - Reconcile
                    - Fail
                    type: string
                type: object
              ownership:
                properties:
                  mode:
                    default: Managed
                    enum:
                    - Managed
                    - InventoryOnly
                    - Hybrid
                    type: string
                  prune:
                    type: boolean
                type: object
              parameterContextRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              reconciliation:
                properties:
                  paused:
                    type: boolean
                type: object
              rollback:
                properties:
                  enabled:
                    type: boolean
                  historyLimit:
                    default: 5
                    format: int32
                    maximum: 20
                    minimum: 1
                    type: integer
                  onFailure:
                    default: PreviousSuccessful
                    enum:
                    - PreviousSuccessful
                    type: string
                type: object
              rollout:
                properties:
                  blueGreen:
                    description: |-
                      BlueGreen tunes the transactional BlueGreen rollout. It is ignored unless
                      strategy is BlueGreen.
                    properties:
                      drainTimeoutSeconds:
                        default: 120
                        description: |-
                          DrainTimeoutSeconds bounds how long the operator waits for a boundary connection
                          queue to drain before applying onDrainTimeout.
                        format: int32
                        minimum: 0
                        type: integer
                      onDrainTimeout:
                        default: Fail
                        description: |-
                          OnDrainTimeout selects the behaviour when a boundary queue has not drained within
                          drainTimeoutSeconds: Fail aborts and switches traffic back to blue; Drop discards
                          the remaining flow files and proceeds.
                        enum:
                        - Fail
                        - Drop
                        type: string
                      readinessTimeoutSeconds:
                        default: 300
                        description: |-
                          ReadinessTimeoutSeconds bounds how long to wait for the candidate to become valid
                          (no invalid components) before failing the rollout.
                        format: int32
                        minimum: 0
                        type: integer
                      requireEnabledControllerServices:
                        default: true
                        description: |-
                          RequireEnabledControllerServices enables and waits for the candidate's controller
                          services before validation.
                        type: boolean
                    type: object
                  strategy:
                    default: ApplyOnly
                    enum:
                    - ApplyOnly
                    - StopAllThenApply
                    - ChangedOnly
                    - Rolling
                    - BlueGreen
                    type: string
                type: object
              source:
                properties:
                  bundleRef:
                    properties:
                      name:
                        minLength: 1
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                  inline:
                    properties:
                      git:
                        properties:
                          credentials:
                            properties:
                              caSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              insecureSkipVerify:
                                type: boolean
                              passwordSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              tokenSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              usernameSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                            x-kubernetes-validations:
                            - message: username and password must be configured together
                              rule: has(self.usernameSecretKeyRef) == has(self.passwordSecretKeyRef)
                            - message: token and username/password authentication
                                are mutually exclusive
                              rule: '!(has(self.tokenSecretKeyRef) && (has(self.usernameSecretKeyRef)
                                || has(self.passwordSecretKeyRef)))'
                          path:
                            type: string
                          ref:
                            type: string
                          url:
                            minLength: 1
                            type: string
                        required:
                        - url
                        type: object
                      oci:
                        properties:
                          credentials:
                            properties:
                              caSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              insecureSkipVerify:
                                type: boolean
                              passwordSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              tokenSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              usernameSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                            x-kubernetes-validations:
                            - message: username and password must be configured together
                              rule: has(self.usernameSecretKeyRef) == has(self.passwordSecretKeyRef)
                            - message: token and username/password authentication
                                are mutually exclusive
                              rule: '!(has(self.tokenSecretKeyRef) && (has(self.usernameSecretKeyRef)
                                || has(self.passwordSecretKeyRef)))'
                          digest:
                            type: string
                          image:
                            minLength: 1
                            type: string
                          path:
                            description: Path is the snapshot file in the OCI image
                              filesystem and defaults to flow.json.
                            type: string
                        required:
                        - image
                        type: object
                      registry:
                        properties:
                          bucketId:
                            minLength: 1
                            type: string
                          credentials:
                            properties:
                              caSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              insecureSkipVerify:
                                type: boolean
                              passwordSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              tokenSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              usernameSecretKeyRef:
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                            x-kubernetes-validations:
                            - message: username and password must be configured together
                              rule: has(self.usernameSecretKeyRef) == has(self.passwordSecretKeyRef)
                            - message: token and username/password authentication
                                are mutually exclusive
                              rule: '!(has(self.tokenSecretKeyRef) && (has(self.usernameSecretKeyRef)
                                || has(self.passwordSecretKeyRef)))'
                          flowId:
                            minLength: 1
                            type: string
                          registryClientRef:
                            properties:
                              name:
                                minLength: 1
                                type: string
                              namespace:
                                type: string
                            required:
                            - name
                            type: object
                          version:
                            type: string
                        required:
                        - bucketId
                        - flowId
                        - registryClientRef
                        type: object
                      snapshot:
                        description: |-
                          Snapshot contains a NiFi RegisteredFlowSnapshot, such as the JSON
                          returned by downloading a versioned process group.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one flow bundle source must be configured
                      rule: '(has(self.git) ? 1 : 0) + (has(self.oci) ? 1 : 0) + (has(self.registry)
                        ? 1 : 0) + (has(self.snapshot) ? 1 : 0) == 1'
                  version:
                    type: string
                type: object
                x-kubernetes-validations:
                - message: exactly one flow deployment source must be configured
                  rule: '(has(self.bundleRef) ? 1 : 0) + (has(self.inline) ? 1 : 0)
                    == 1'
              target:
                properties:
                  parentProcessGroupRef:
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      root:
                        type: boolean
                    type: object
                  processGroupName:
                    type: string
                type: object
            required:
            - clusterRef
            - source
            - target
            type: object
          status:
            properties:
              activeRollout:
                properties:
                  blueGreen:
                    description: BlueGreen carries the transactional BlueGreen rollout
                      state between reconciles.
                    properties:
                      blueProcessGroupId:
                        description: BlueProcessGroupID is the live process group
                          being replaced.
                        type: string
                      candidateProcessGroupId:
                        description: CandidateProcessGroupID is the green process
                          group being promoted.
                        type: string
                      externalConnections:
                        description: |-
                          ExternalConnections is the recorded boundary connection inventory used to switch
                          traffic to green and, on failure, switch it back to blue.
                        items:
                          description: |-
                            ExternalConnectionRecord captures a boundary connection crossing the deployment's
                            process-group edge so it can be recreated against the green ports and, on rollback,
                            restored against the blue ports.
                          properties:
                            definition:
                              description: Definition is the JSON connection component
                                recorded for faithful recreation.
                              type: string
                            direction:
                              description: |-
                                Direction is Inbound (external source -> deployment input port) or Outbound
                                (deployment output port -> external destination).
                              type: string
                            drainStartedAt:
                              description: |-
                                DrainStartedAt marks when draining of this connection began, bounding the wait
                                against the configured drain timeout.
                              format: date-time
                              type: string
                            greenConnectionId:
                              description: GreenConnectionID is the id of the connection
                                created against the green port.
                              type: string
                            originalId:
                              description: OriginalID is the connection id as it existed
                                against blue.
                              type: string
                            portName:
                              description: |-
                                PortName is the deployment-side input or output port name used to match blue and
                                green ports.
                              type: string
                            switched:
                              description: Switched indicates the connection has been
                                re-pointed to the green port.
                              type: boolean
                          type: object
                        type: array
                      inventoried:
                        description: Inventoried marks that the external boundary
                          connections have been recorded.
                        type: boolean
                      parentProcessGroupId:
                        description: ParentProcessGroupID is the parent that owns
                          the boundary connections.
                        type: string
                    type: object
                  operation:
                    type: string
                  phase:
                    type: string
                  previousDigest:
                    type: string
                  previousVersion:
                    type: string
                  startedAt:
                    format: date-time
                    type: string
                  strategy:
                    type: string
                  targetDigest:
                    type: string
                  targetVersion:
                    type: string
                type: object
              artifactDigest:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dependencies:
                properties:
                  ready:
                    type: boolean
                  waitingFor:
                    items:
                      type: string
                    type: array
                type: object
              deployedVersion:
                type: string
              desiredContentDigest:
                type: string
              drift:
                properties:
                  differences:
                    items:
                      type: string
                    type: array
                  lastDetectedTime:
                    format: date-time
                    type: string
                  status:
                    type: string
                type: object
              lastRollback:
                properties:
                  completedAt:
                    format: date-time
                    type: string
                  failedDigest:
                    type: string
                  failedGeneration:
                    format: int64
                    type: integer
                  failedVersion:
                    type: string
                  message:
                    type: string
                  restoredDigest:
                    type: string
                  restoredVersion:
                    type: string
                type: object
              lastSuccessfulDeployment:
                properties:
                  deployedAt:
                    format: date-time
                    type: string
                  digest:
                    type: string
                  reason:
                    type: string
                  result:
                    type: string
                  snapshotConfigMap:
                    type: string
                  strategy:
                    type: string
                  version:
                    type: string
                type: object
              latestReplaceRequest:
                properties:
                  complete:
                    type: boolean
                  failureReason:
                    type: string
                  id:
                    type: string
                  operation:
                    type: string
                  percentCompleted:
                    format: int32
                    type: integer
                  snapshotConfigMap:
                    type: string
                  state:
                    type: string
                  targetDigest:
                    type: string
                  targetVersion:
                    type: string
                type: object
              liveContentDigest:
                type: string
              nifiId:
                type: string
              observedGeneration:
                format: int64
                type: integer
              processGroupId:
                type: string
              ready:
                type: boolean
              retiringProcessGroupId:
                description: |-
                  RetiringProcessGroupID is a promoted-away (blue) process group kept for one
                  reconcile after a BlueGreen switch, then deleted.
                type: string
              revision:
                properties:
                  clientId:
                    type: string
                  version:
                    format: int64
                    type: integer
                type: object
              rolloutHistory:
                items:
                  properties:
                    deployedAt:
                      format: date-time
                      type: string
                    digest:
                      type: string
                    reason:
                      type: string
                    result:
                      type: string
                    snapshotConfigMap:
                      type: string
                    strategy:
                      type: string
                    version:
                      type: string
                  type: object
                type: array
              sync:
                properties:
                  lastAttemptTime:
                    format: date-time
                    type: string
                  lastError:
                    type: string
                  lastSuccessfulTime:
                    format: date-time
                    type: string
                type: object
              syncState:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
